//тернарный оператор - условие(проверка задачи) ? выражение1 (если условие True) : выражение2 (если условие False)

const age = 18

const isAdult = age >= 18 ? 'Adult' : 'Minor'

console.log(isAdult)
